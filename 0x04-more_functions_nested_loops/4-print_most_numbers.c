#include "main.h"

/**
 * print_most_numbers - prints numbers between 0 to 9
 *(neither 2 nor 4)
 * Return: no return
 */
void print_ most_numbers(void)
{
	char ch;

	for (ch = 48; ch <= 58; c++)
	{
		if (c != 50 && ch != 52)
			_putchar(ch);
	}
	_putchar('\n');
}
